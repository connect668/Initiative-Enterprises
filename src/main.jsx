import React from "react";
import ReactDOM from "react-dom/client";
import FreshTest from "./FreshTest.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FreshTest />
  </React.StrictMode>
);
